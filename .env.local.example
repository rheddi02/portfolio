# ── Resend (contact form email delivery) ──────────────────────────
# Get your API key at https://resend.com
# In development, leave this blank — the API route will skip sending
# and log a warning instead of erroring out.
RESEND_API_KEY=
